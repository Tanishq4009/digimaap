/// The three physical seal shapes the overlay guide supports.
/// Kept in its own file so both the capture screen and the cropper
/// service can import it without a circular dependency.
enum SealType { lead, punch, sticker }
